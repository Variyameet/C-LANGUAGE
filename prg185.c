// find area of squear
// no pera and no return
// with pera no return
// with pera with return    ==>
// no pera with return

#include<stdio.h>

int area(int l)
{

   return l*l;
}

int main()
{
       int l,as;
    int area(int);

     printf("enter the length");
    scanf("%d",&l);

    printf("simple intrest : %d",area(l));

    return 0;
}
